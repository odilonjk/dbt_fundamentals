name: 'chinese_fine_art'

config-version: 2
version: '0.1'

profile: 'chinese_fine_art'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

models:
  chinese_fine_art:
    staging:
      +materialized: view
    marts:
      +materialized: table