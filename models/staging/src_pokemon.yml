version: 2

sources:
  - name: pokemon
    database: pokemon
    schema: public
    tables:
      - name: raw_caught_attempt
        columns:
          - name: pokedex_number
            tests:
              - not_null
      - name: raw_cities
      - name: raw_pokemon
      - name: raw_trainers
