config:
  send_anonymous_usage_stats: False

pokemon:
  target: dev
  outputs:
    dev:
      type: postgres
      host: host.docker.internal
      user: postgres
      password: postgres
      port: 5432
      dbname: pokemon
      schema: public